import React from 'react';

import classes from './NoInternetConnection.css';

const noInternetConnection = () => (
  <div className={classes.NoInternetConnection}>
    <h1>We`re sorry, but there is no internet connection.</h1>
  </div>
);

export default noInternetConnection;
